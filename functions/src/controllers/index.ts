import { createCandidate } from "./candidates";
import { createJob } from "./jobs";

const controller = {
  candidate: {
    createCandidate,
  },
  job: {
    createJob,
  },
};

export default controller;
